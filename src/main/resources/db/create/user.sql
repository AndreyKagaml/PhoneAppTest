CREATE TABLE `user`(
                       `id_user` INT NOT NULL AUTO_INCREMENT,
                       `name` VARCHAR(50) NOT NULL,
                       `password` VARCHAR(100) NOT NULL,
                       PRIMARY KEY(`id_user`)
)